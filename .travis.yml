arch: 
  - ppc64le
sudo: required
services:
  - docker

script:
  - ls -l /
  - docker run -t ubuntu:18.04 bash -c 'ls -l /'
  - docker run --privileged -t ubuntu:18.04 bash -c 'ls -l /'
  - docker ps -a
